"use strict";(globalThis.webpackChunklhtp=globalThis.webpackChunklhtp||[]).push([[59265],{85368(e,t,n){n.r(t),n.d(t,{assets:()=>c,contentTitle:()=>a,default:()=>h,frontMatter:()=>o,metadata:()=>i,toc:()=>d});const i=JSON.parse('{"id":"react/react-with-nosql/3-4-2-2-adding-wait-time-to-the-queue","title":"\ud83d\udcd3 3.4.2.2 Adding Wait Time to the Queue","description":"In this lesson we\'re going to work with the Firestore server timestamp to add a wait time to our Help Queue project. For this refactor, we\'ll need to complete a few steps:","source":"@site/docs/3_react/4_react-with-nosql/3-4-2-02-adding-wait-time-to-the-queue.md","sourceDirName":"3_react/4_react-with-nosql","slug":"/react/react-with-nosql/3-4-2-2-adding-wait-time-to-the-queue","permalink":"/v1.4/react/react-with-nosql/3-4-2-2-adding-wait-time-to-the-queue","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"\ud83d\udcd3 3.4.2.2 Adding Wait Time to the Queue","day":"tuesday","id":"3-4-2-2-adding-wait-time-to-the-queue","hide_table_of_contents":true},"sidebar":"react","previous":{"title":"\ud83d\udcd3 3.4.2.1 Firestore Queries","permalink":"/v1.4/react/react-with-nosql/3-4-2-1-firestore-queries"},"next":{"title":"\ud83d\udcd3 3.4.2.3 Hosting with Firebase","permalink":"/v1.4/react/react-with-nosql/3-4-2-3-hosting-with-firebase"}}');var s=n(74848),r=n(28453);const o={title:"\ud83d\udcd3 3.4.2.2 Adding Wait Time to the Queue",day:"tuesday",id:"3-4-2-2-adding-wait-time-to-the-queue",hide_table_of_contents:!0},a=void 0,c={},d=[{value:"Updating Our Types",id:"updating-our-types",level:2},{value:"Adding a Server Timestamp at Ticket Creation",id:"adding-a-server-timestamp-at-ticket-creation",level:2},{value:"Adding a Formatted Wait Time",id:"adding-a-formatted-wait-time",level:2},{value:"Keeping the Wait Time Fresh",id:"keeping-the-wait-time-fresh",level:3},{value:"Computing and Displaying the Wait Time in <code>Ticket</code>",id:"computing-and-displaying-the-wait-time-in-ticket",level:3},{value:"Keeping <code>EditTicketForm</code> Compiling",id:"keeping-editticketform-compiling",level:3},{value:"Ordering Tickets by Creation Timestamp",id:"ordering-tickets-by-creation-timestamp",level:2}];function l(e){const t={a:"a",admonition:"admonition",code:"code",em:"em",h2:"h2",h3:"h3",hr:"hr",li:"li",mdxadmonitiontitle:"mdxadmonitiontitle",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(t.p,{children:"In this lesson we're going to work with the Firestore server timestamp to add a wait time to our Help Queue project. For this refactor, we'll need to complete a few steps:"}),"\n",(0,s.jsxs)(t.ol,{children:["\n",(0,s.jsxs)(t.li,{children:["We'll use a new function called ",(0,s.jsx)(t.code,{children:"serverTimestamp()"})," to generate a timestamp when a ticket is initially created. This will be the exact time when the ticket is added to our database."]}),"\n",(0,s.jsxs)(t.li,{children:["Then, we'll use this timestamp to generate a formatted wait time using the ",(0,s.jsx)(t.code,{children:"date-fns"})," library, computed fresh each time a ticket renders."]}),"\n",(0,s.jsxs)(t.li,{children:["Finally, we'll set up a ",(0,s.jsx)(t.code,{children:"useEffect()"})," hook with a ",(0,s.jsx)(t.code,{children:"setInterval()"})," function that re-renders the queue every minute, so the formatted wait time stays current."]}),"\n"]}),"\n",(0,s.jsx)(t.p,{children:"We'll also learn how to use a Firestore timestamp to order our tickets from oldest to newest. Why bother? Well, we're making use of timestamps for another scenario: preserving the creation order for every document in a collection."}),"\n",(0,s.jsx)(t.p,{children:"Previously we learned that the auto-generated IDs from Firestore are always random and they do not include any reference as to the order in which each document was created. However, Firestore still orders the documents in a collection alphabetically by its identifier, with numbers taking precedence over letters. This means that the order in which documents appear in the database and our website is subject to change anytime a new document is added. That's no good. So, we'll solve that issue in this lesson, and we'll do so with the help of server timestamps."}),"\n",(0,s.jsx)(t.p,{children:"Let's get into this refactor!"}),"\n",(0,s.jsx)(t.h2,{id:"updating-our-types",children:"Updating Our Types"}),"\n",(0,s.jsx)(t.hr,{}),"\n",(0,s.jsxs)(t.p,{children:["Every ticket is about to carry one new piece of information: the moment it was created. Before we write any component code, let's say so in ",(0,s.jsx)(t.code,{children:"src/types.ts"}),". Doing the types first is a habit worth building: it forces us to decide what shape our data has before we start moving it around."]}),"\n",(0,s.jsx)(t.p,{children:"Here's the updated file:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-ts",metastring:'title="src/types.ts"',children:"// highlight-next-line\nimport { type FieldValue } from 'firebase/firestore';\n\nexport type TicketData = {\n  names: string;\n  section: string;\n  issue: string;\n  id: string;\n  // highlight-next-line\n  timeOpen: Date;\n};\n\n// highlight-start\nexport type NewTicketData = {\n  names: string;\n  section: string;\n  issue: string;\n  timeOpen: FieldValue;\n};\n// highlight-end\n"})}),"\n",(0,s.jsx)(t.p,{children:"There's a lot packed into that small file, so let's work through it."}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsxs)(t.strong,{children:[(0,s.jsx)(t.code,{children:"TicketData"})," gains one field."]})," ",(0,s.jsx)(t.code,{children:"timeOpen"})," is a ",(0,s.jsx)(t.code,{children:"Date"}),", because by the time a ticket reaches our components we'll have converted the Firestore timestamp into a JavaScript ",(0,s.jsx)(t.code,{children:"Date"})," object. We'll compute a human-readable wait time from it later, but we won't store that string on the ticket itself - more on why in a moment."]}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsxs)(t.strong,{children:[(0,s.jsx)(t.code,{children:"NewTicketData"})," is no longer built with ",(0,s.jsx)(t.code,{children:"Omit"}),"."]})," This is the interesting part. Up to now, ",(0,s.jsx)(t.code,{children:"NewTicketData"})," was just ",(0,s.jsx)(t.code,{children:'Omit<TicketData, "id">'}),": the exact same fields, minus the one Firestore assigns. That worked because a ticket looked identical going into the database and coming back out."]}),"\n",(0,s.jsxs)(t.p,{children:["That's no longer true. On the way ",(0,s.jsx)(t.em,{children:"in"}),", ",(0,s.jsx)(t.code,{children:"timeOpen"})," isn't a date at all. It's the value returned by ",(0,s.jsx)(t.code,{children:"serverTimestamp()"}),', which is a placeholder that tells Firestore "fill this in with the time you receive this write." Firebase types that placeholder as ',(0,s.jsx)(t.code,{children:"FieldValue"}),"."]}),"\n",(0,s.jsxs)(t.p,{children:["So the two types genuinely describe ",(0,s.jsx)(t.code,{children:"timeOpen"})," differently now, and writing ",(0,s.jsx)(t.code,{children:"NewTicketData"})," out in full is the clearest way to say that. We could express the relationship with an intersection instead:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-ts",children:'export type NewTicketData = Omit<TicketData, "id" | "timeOpen"> & {\n  timeOpen: FieldValue;\n};\n'})}),"\n",(0,s.jsxs)(t.p,{children:["Both versions produce the same type. The second one stays in sync automatically if we add a field to ",(0,s.jsx)(t.code,{children:"TicketData"})," later, at the cost of being harder to read at a glance. We'll use the explicit version in this lesson."]}),"\n",(0,s.jsxs)(t.admonition,{type:"tip",children:[(0,s.jsxs)(t.mdxadmonitiontitle,{children:["Why not just use ",(0,s.jsx)(t.code,{children:"Date"})," for the incoming timestamp?"]}),(0,s.jsxs)(t.p,{children:["We could create the timestamp ourselves with ",(0,s.jsx)(t.code,{children:"new Date()"})," and skip ",(0,s.jsx)(t.code,{children:"FieldValue"})," entirely. The reason we don't is that ",(0,s.jsx)(t.code,{children:"new Date()"})," reads the clock on the ",(0,s.jsx)(t.em,{children:"user's"})," machine, which may be wrong, in a different time zone, or deliberately set to something odd. ",(0,s.jsx)(t.code,{children:"serverTimestamp()"})," asks Firestore's servers for the time instead, so every ticket in the queue is stamped by the same clock. When you need timestamps that different users can be compared against each other, that difference matters."]})]}),"\n",(0,s.jsx)(t.h2,{id:"adding-a-server-timestamp-at-ticket-creation",children:"Adding a Server Timestamp at Ticket Creation"}),"\n",(0,s.jsx)(t.hr,{}),"\n",(0,s.jsx)(t.p,{children:"The first thing we'll do in this refactor is create a server timestamp when a ticket is first created. Later on, we'll use this value to calculate a formatted time that shows how long a ticket has been open."}),"\n",(0,s.jsxs)(t.p,{children:["To add a Firestore server timestamp to new tickets, we'll need to update ",(0,s.jsx)(t.code,{children:"NewTicketForm.tsx"}),". Here's the new code:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/NewTicketForm.tsx"',children:"import { type SubmitEvent } from 'react';\nimport { type NewTicketData } from '../types';\nimport ReusableForm from './ReusableForm';\n// highlight-next-line\nimport { serverTimestamp } from 'firebase/firestore';\n\ntype NewTicketFormProps = {\n  onNewTicketCreation: (ticket: NewTicketData) => void;\n};\n\nfunction NewTicketForm({ onNewTicketCreation }: NewTicketFormProps) {\n\n  function handleNewTicketFormSubmission(event: SubmitEvent<HTMLFormElement>) {\n    event.preventDefault();\n    const formData = new FormData(event.currentTarget);\n    onNewTicketCreation({\n      names: formData.get('names') as string,\n      section: formData.get('section') as string,\n      issue: formData.get('issue') as string,\n      // highlight-next-line\n      timeOpen: serverTimestamp()\n    });\n  }\n\n  return (\n    <ReusableForm\n      formSubmissionHandler={handleNewTicketFormSubmission}\n      buttonText=\"Help!\"\n    />\n  );\n}\n\nexport default NewTicketForm;\n"})}),"\n",(0,s.jsxs)(t.p,{children:["We've added a new import to get access to the ",(0,s.jsx)(t.code,{children:"serverTimestamp()"})," function from ",(0,s.jsx)(t.code,{children:"firebase/firestore"}),". Then, we've added a new ",(0,s.jsx)(t.code,{children:"timeOpen"})," property that's set to the ",(0,s.jsx)(t.code,{children:"serverTimestamp()"}),":"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"timeOpen: serverTimestamp()\n"})}),"\n",(0,s.jsxs)(t.p,{children:["As you may guess, the ",(0,s.jsx)(t.code,{children:"serverTimestamp()"})," function returns a value that Firestore replaces with a timestamp corresponding to when the ticket gets added as a document in the database."]}),"\n",(0,s.jsxs)(t.p,{children:["Notice that we didn't have to change the prop type at all. ",(0,s.jsx)(t.code,{children:"onNewTicketCreation"})," is still typed as ",(0,s.jsx)(t.code,{children:"(ticket: NewTicketData) => void"}),", and because we already updated ",(0,s.jsx)(t.code,{children:"NewTicketData"})," to include ",(0,s.jsx)(t.code,{children:"timeOpen"}),", TypeScript accepts the new property here and would have complained if we'd left it out. Because ",(0,s.jsx)(t.code,{children:"handleAddingNewTicketToList"})," in ",(0,s.jsx)(t.code,{children:"TicketControl"})," also takes a ",(0,s.jsx)(t.code,{children:"NewTicketData"}),", that side of the connection is already correct too. Doing the type work first meant this component change was the only edit we needed."]}),"\n",(0,s.jsx)(t.h2,{id:"adding-a-formatted-wait-time",children:"Adding a Formatted Wait Time"}),"\n",(0,s.jsx)(t.hr,{}),"\n",(0,s.jsxs)(t.p,{children:["The next step is to add a formatted wait time that displays how long a ticket has been open. We'll start by converting the raw Firestore timestamp into a ",(0,s.jsx)(t.code,{children:"Date"})," in ",(0,s.jsx)(t.code,{children:"TicketControl.tsx"}),". Then we'll compute and display the actual formatted wait time in ",(0,s.jsx)(t.code,{children:"Ticket.tsx"}),", where it's rendered."]}),"\n",(0,s.jsxs)(t.p,{children:["We'll start by installing the ",(0,s.jsx)(t.code,{children:"date-fns"})," library, a popular JavaScript library for working with dates and time. We can use date-fns to manipulate and parse time, which is exactly what we'll use it for."]}),"\n",(0,s.jsx)(t.p,{children:"In the root directory of your Help Queue project, run the following command:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-bash",children:"npm install date-fns@4\n"})}),"\n",(0,s.jsxs)(t.p,{children:["date-fns ships its own TypeScript definitions, so there's no separate ",(0,s.jsx)(t.code,{children:"@types"})," package to install. As soon as it's installed, your editor knows what each of its functions accepts and returns."]}),"\n",(0,s.jsxs)(t.p,{children:["We'll use the ",(0,s.jsx)(t.code,{children:"formatDistanceToNow()"})," helper function from ",(0,s.jsx)(t.code,{children:"date-fns"}),' to display a human-readable time like "about 5 minutes ago". The ',(0,s.jsx)(t.a,{href:"https://date-fns.org/docs/Getting-Started",children:"documentation for date-fns"})," is extensive, and there are many other helper functions available. We recommend checking it out when you have the time, as there are many use cases where it can add valuable functionality to an application."]}),"\n",(0,s.jsxs)(t.p,{children:["This is how we'll use ",(0,s.jsxs)(t.a,{href:"https://date-fns.org/v4.4.0/docs/formatDistanceToNow",children:["the ",(0,s.jsx)(t.code,{children:"formatDistanceToNow"})," helper function"]}),":"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"formatDistanceToNow(new Date());\n"})}),"\n",(0,s.jsx)(t.p,{children:'This time, we\'re not going to include the options object that will add "ago" to the end of the formatted time, like "7 minutes ago". You can add it if you like. This is what the syntax looks like:'}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"formatDistanceToNow(new Date(), {\n  addSuffix: true\n});\n"})}),"\n",(0,s.jsxs)(t.p,{children:["Since ",(0,s.jsx)(t.code,{children:"formatDistanceToNow()"})," takes a JavaScript ",(0,s.jsx)(t.code,{children:"Date"})," object as its first argument, ",(0,s.jsx)(t.code,{children:"timeOpen"})," needs to already be a ",(0,s.jsx)(t.code,{children:"Date"})," by the time it reaches the component that calls it. Let's convert the Firestore timestamp into a ",(0,s.jsx)(t.code,{children:"Date"})," in ",(0,s.jsx)(t.code,{children:"TicketControl.tsx"})," first, before we get to where ",(0,s.jsx)(t.code,{children:"formatDistanceToNow()"})," is actually called:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/TicketControl.tsx"',children:"// ...other imports\n\nfunction TicketControl() {\n  // ...other state declarations\n\n  useEffect(() => {\n    const unSubscribe = onSnapshot(\n      collection(db, \"tickets\"),\n      (collectionSnapshot) => {\n        const tickets: TicketData[] = [];\n        collectionSnapshot.forEach((doc) => {\n          // highlight-next-line\n          const data = doc.data({ serverTimestamps: 'estimate' });\n          // highlight-next-line\n          const jsDate = data.timeOpen.toDate();\n          tickets.push({\n            names: data.names,\n            section: data.section,\n            issue: data.issue,\n            // highlight-next-line\n            timeOpen: jsDate,\n            id: doc.id\n          });\n        });\n        setMainTicketList(tickets);\n      },\n      (firestoreError) => {\n        setError(firestoreError.message);\n      }\n    );\n\n    return () => unSubscribe();\n  }, []);\n\n  // ...rest of TicketControl\n}\n\nexport default TicketControl;\n"})}),"\n",(0,s.jsxs)(t.p,{children:["Let's break down this new code. First we go through the process of turning the Firestore server timestamp into a ",(0,s.jsx)(t.code,{children:"Date"})," object:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"const data = doc.data({ serverTimestamps: 'estimate' });\nconst jsDate = data.timeOpen.toDate();\n"})}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.code,{children:"serverTimestamps"})," matters specifically for fields created with ",(0,s.jsx)(t.code,{children:"serverTimestamp()"}),", like ",(0,s.jsx)(t.code,{children:"timeOpen"}),". Right after a ticket is created, there's a brief window before Firestore's server confirms the write and reports back the real timestamp; until then, the field would otherwise just be ",(0,s.jsx)(t.code,{children:"null"}),". Passing ",(0,s.jsx)(t.code,{children:"'estimate'"})," tells Firestore to fill that gap with a timestamp based on the local device's clock instead, so ",(0,s.jsx)(t.code,{children:"timeOpen"})," always has a usable value. ",(0,s.jsx)(t.code,{children:"doc.data()"})," accepts this same option as ",(0,s.jsx)(t.code,{children:"doc.get()"}),", so we can pass it once here and read every field, including ",(0,s.jsx)(t.code,{children:"timeOpen"}),", off the same ",(0,s.jsx)(t.code,{children:"data"})," object. ",(0,s.jsx)(t.code,{children:"data.timeOpen"})," is a ",(0,s.jsxs)(t.a,{href:"https://firebase.google.com/docs/reference/js/firestore_.timestamp.md",children:["Firestore ",(0,s.jsx)(t.code,{children:"Timestamp"})," object"]}),"; calling ",(0,s.jsx)(t.code,{children:"Timestamp.toDate()"})," on it hands us back a plain JavaScript ",(0,s.jsx)(t.code,{children:"Date"}),"."]}),"\n",(0,s.jsxs)(t.p,{children:["If you compare this with earlier versions of this code, you may also notice the redundant ",(0,s.jsx)(t.code,{children:"new Date(timeOpen)"})," wrapper is gone: since ",(0,s.jsx)(t.code,{children:"toDate()"})," already returns a ",(0,s.jsx)(t.code,{children:"Date"}),", that wrapper did nothing, so we've dropped it."]}),"\n",(0,s.jsxs)(t.admonition,{title:"Delete your old tickets first",type:"caution",children:[(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.code,{children:"data.timeOpen.toDate()"})," will throw a runtime error for any ticket document that has no ",(0,s.jsx)(t.code,{children:"timeOpen"})," field, because ",(0,s.jsx)(t.code,{children:"undefined"})," has no ",(0,s.jsx)(t.code,{children:"toDate()"})," method. Every ticket you created before this lesson is in exactly that state."]}),(0,s.jsx)(t.p,{children:"Go delete any old tickets from the Firebase console before testing. Doing this will also help us avoid errors when we sort the Firestore tickets by their creation date later in this lesson."}),(0,s.jsxs)(t.p,{children:["This is the same boundary problem we discussed when we first read documents out of Firestore. ",(0,s.jsx)(t.code,{children:"doc.data()"})," reaches into a document whose shape TypeScript knows nothing about, so nothing here can be checked at compile time. Our types describe what we ",(0,s.jsx)(t.em,{children:"intend"})," each document to contain; keeping the database's actual contents in line with that intention is our job."]})]}),"\n",(0,s.jsxs)(t.p,{children:["We then add the new ",(0,s.jsx)(t.code,{children:"timeOpen"})," property to our ticket object:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"tickets.push({\n  // ...names, section, issue as before\n  timeOpen: jsDate,\n  // ...id as before\n});\n"})}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"timeOpen"})," property is set to the ",(0,s.jsx)(t.code,{children:"jsDate"})," variable. It's now required by ",(0,s.jsx)(t.code,{children:"TicketData"}),", so if we had left it out, the ",(0,s.jsx)(t.code,{children:"tickets.push()"})," call would have failed to compile and pointed us straight at the missing field."]}),"\n",(0,s.jsxs)(t.admonition,{title:"If you used the spread-operator listener from Viewing Tickets from Firestore",type:"note",children:[(0,s.jsxs)(t.p,{children:["In ",(0,s.jsx)(t.a,{href:"../../react/react-with-nosql/3-4-0-13-viewing-tickets-from-firestore",children:"Viewing Tickets from Firestore"}),", we mentioned that the optional spread-operator version of this listener (",(0,s.jsx)(t.code,{children:'...doc.data() as Omit<TicketData, "id">'}),") happened to describe the same shape as ",(0,s.jsx)(t.code,{children:"NewTicketData"}),", but wouldn't stay that way forever. This is where they part ways: ",(0,s.jsx)(t.code,{children:"timeOpen"})," needs to go through ",(0,s.jsx)(t.code,{children:"{ serverTimestamps: 'estimate' }"})," and ",(0,s.jsx)(t.code,{children:".toDate()"})," before it matches ",(0,s.jsx)(t.code,{children:"TicketData"}),", and a type assertion can't do that conversion for you - it would just tell TypeScript to trust that a raw Firestore ",(0,s.jsx)(t.code,{children:"Timestamp"})," (or ",(0,s.jsx)(t.code,{children:"null"}),", mid-write) is already a ",(0,s.jsx)(t.code,{children:"Date"}),", when it isn't."]}),(0,s.jsx)(t.p,{children:"If you're using the spread version, switch back to listing fields out individually, as we do in this lesson. The field-by-field version already handles this conversion correctly."})]}),"\n",(0,s.jsxs)(t.p,{children:["Why not leave ",(0,s.jsx)(t.code,{children:"timeOpen"})," as a Firestore ",(0,s.jsx)(t.code,{children:"Timestamp"}),"? We'll pass ",(0,s.jsx)(t.code,{children:"timeOpen"})," down to ",(0,s.jsx)(t.code,{children:"Ticket"})," in a moment, where ",(0,s.jsx)(t.code,{children:"formatDistanceToNow()"})," will use it to render a wait time, and that function wants a ",(0,s.jsx)(t.code,{children:"Date"}),". Converting once, at the boundary where data enters our app, means every component downstream gets to work with an ordinary ",(0,s.jsx)(t.code,{children:"Date"}),". That's a pattern worth remembering: convert external data into your own types as early as possible, so the rest of your code never has to think about where it came from."]}),"\n",(0,s.jsx)(t.h3,{id:"keeping-the-wait-time-fresh",children:"Keeping the Wait Time Fresh"}),"\n",(0,s.jsxs)(t.p,{children:["There's a wrinkle with computing a wait time this way: ",(0,s.jsx)(t.code,{children:"formatDistanceToNow()"}),' produces a string that goes stale. A ticket rendered the moment it opens says "less than a minute", but five minutes later, if nothing else has changed, React has no reason to re-render that ticket, so the page would still say "less than a minute".']}),"\n",(0,s.jsxs)(t.p,{children:["It might be tempting to store the formatted string in state and recompute it on a timer, but that means keeping two pieces of state - ",(0,s.jsx)(t.code,{children:"timeOpen"})," and a string derived from it - in sync by hand, and they could in principle drift apart. Instead, we'll compute the formatted string directly where ",(0,s.jsx)(t.code,{children:"Ticket"})," renders (we'll get there in the next section), and use a ",(0,s.jsx)(t.code,{children:"setInterval()"})," here just to periodically force ",(0,s.jsx)(t.code,{children:"TicketControl"}),' to re-render, so that computation reruns with a fresh idea of "now".']}),"\n",(0,s.jsx)(t.p,{children:"Here's the new code:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/TicketControl.tsx"',children:"// ...other imports\n\nfunction TicketControl() {\n  const [formVisibleOnPage, setFormVisibleOnPage] = useState(false);\n  const [mainTicketList, setMainTicketList] = useState<TicketData[]>([]);\n  // ...other state declarations\n  // highlight-next-line\n  const [, setNow] = useState(new Date());\n\n  // ...existing useEffect for the Firestore listener\n\n  // highlight-start\n  useEffect(() => {\n    const waitTimeInterval = setInterval(() => setNow(new Date()), 60000);\n\n    return function cleanup() {\n      clearInterval(waitTimeInterval);\n    }\n  }, []);\n  // highlight-end\n\n  // ...rest of TicketControl\n}\n\nexport default TicketControl;\n"})}),"\n",(0,s.jsxs)(t.p,{children:["We add a new state variable, but notice the empty spot before ",(0,s.jsx)(t.code,{children:"setNow"})," in the destructuring, and that we never reference ",(0,s.jsx)(t.code,{children:"now"})," anywhere. ",(0,s.jsx)(t.code,{children:"useState()"})," always returns an array of two items, and skipping the first with a bare comma is a normal way to say \"give me the setter, but I don't need to name the current value.\" That's intentional here: this state exists purely to give React a reason to re-render ",(0,s.jsx)(t.code,{children:"TicketControl"})," every minute. When that happens, every ",(0,s.jsx)(t.code,{children:"Ticket"})," in the queue re-renders too, and, as we're about to see, recomputes its own formatted wait time fresh."]}),"\n",(0,s.jsx)(t.admonition,{title:"Why the dependency array is empty",type:"tip",children:(0,s.jsxs)(t.p,{children:["This effect's dependency array is ",(0,s.jsx)(t.code,{children:"[]"}),", not ",(0,s.jsx)(t.code,{children:"[mainTicketList]"}),". The interval doesn't need to know anything about the ticket list - it only calls ",(0,s.jsx)(t.code,{children:"setNow()"}),", which has nothing to do with ",(0,s.jsx)(t.code,{children:"mainTicketList"}),". An empty array means this effect runs once, sets up a single interval, and only tears it down when ",(0,s.jsx)(t.code,{children:"TicketControl"})," unmounts."]})}),"\n",(0,s.jsxs)(t.p,{children:["The cleanup function works the same way it always has: ",(0,s.jsx)(t.code,{children:"clearInterval(waitTimeInterval)"})," stops the interval, which matters because it prevents the creation of multiple intervals every time this effect reruns."]}),"\n",(0,s.jsxs)(t.h3,{id:"computing-and-displaying-the-wait-time-in-ticket",children:["Computing and Displaying the Wait Time in ",(0,s.jsx)(t.code,{children:"Ticket"})]}),"\n",(0,s.jsxs)(t.p,{children:["Next, let's update our ",(0,s.jsx)(t.code,{children:"Ticket"})," component to compute and display the wait time. We'll need ",(0,s.jsx)(t.code,{children:"TicketList.tsx"})," to pass ",(0,s.jsx)(t.code,{children:"timeOpen"})," down as a prop instead, so ",(0,s.jsx)(t.code,{children:"Ticket"})," has the raw data it needs to do its own formatting. Let's start there."]}),"\n",(0,s.jsx)(t.p,{children:"Here's the updated code:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/TicketList.tsx"',children:"import Ticket from './Ticket';\nimport { type TicketData } from '../types';\n\ntype TicketListProps = {\n  ticketList: TicketData[];\n  onTicketSelection: (id: string) => void;\n};\n\nfunction TicketList({ ticketList, onTicketSelection }: TicketListProps) {\n  return (\n    <>\n      <hr />\n      {ticketList.map((ticket) =>\n        <Ticket\n          onTicketClick={onTicketSelection}\n          names={ticket.names}\n          section={ticket.section}\n          // highlight-next-line\n          timeOpen={ticket.timeOpen}\n          issue={ticket.issue}\n          id={ticket.id}\n          key={ticket.id}\n        />\n      )}\n    </>\n  );\n}\n\nexport default TicketList;\n"})}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.code,{children:"TicketListProps"})," doesn't change here. ",(0,s.jsx)(t.code,{children:"ticketList"})," is still a ",(0,s.jsx)(t.code,{children:"TicketData[]"}),", and ",(0,s.jsx)(t.code,{children:"TicketData"})," is the type we already extended, so ",(0,s.jsx)(t.code,{children:"ticket.timeOpen"})," is available to pass along without any further work."]}),"\n",(0,s.jsxs)(t.p,{children:["Next, we'll update ",(0,s.jsx)(t.code,{children:"Ticket.tsx"})," to accept ",(0,s.jsx)(t.code,{children:"timeOpen"})," and compute the formatted wait time itself, right where it's rendered."]}),"\n",(0,s.jsx)(t.p,{children:"Here's the new code:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/Ticket.tsx"',children:"// highlight-next-line\nimport { formatDistanceToNow } from 'date-fns';\n\ntype TicketProps = {\n  names: string;\n  section: string;\n  issue: string;\n  // highlight-next-line\n  timeOpen: Date;\n  id: string;\n  onTicketClick: (id: string) => void;\n};\n\n// highlight-next-line\nfunction Ticket({ names, section, issue, timeOpen, id, onTicketClick }: TicketProps) {\n  return (\n    <div onClick={() => onTicketClick(id)}>\n      <h3>{section} - {names}</h3>\n      <p><em>{issue}</em></p>\n      {/* highlight-next-line */}\n      <p><em>{formatDistanceToNow(timeOpen)}</em></p>\n      <hr />\n    </div>\n  );\n}\n\nexport default Ticket;\n"})}),"\n",(0,s.jsx)(t.p,{children:(0,s.jsx)(t.strong,{children:"What changed:"})}),"\n",(0,s.jsxs)(t.ul,{children:["\n",(0,s.jsxs)(t.li,{children:["We import ",(0,s.jsx)(t.code,{children:"formatDistanceToNow"})," here instead of in ",(0,s.jsx)(t.code,{children:"TicketControl"}),", since this is where we actually use it."]}),"\n",(0,s.jsxs)(t.li,{children:["We add ",(0,s.jsx)(t.code,{children:"timeOpen: Date"})," to ",(0,s.jsx)(t.code,{children:"TicketProps"}),", and destructure ",(0,s.jsx)(t.code,{children:"timeOpen"})," alongside the other props."]}),"\n",(0,s.jsxs)(t.li,{children:["We call ",(0,s.jsx)(t.code,{children:"formatDistanceToNow(timeOpen)"})," directly inside the JSX we render."]}),"\n"]}),"\n",(0,s.jsxs)(t.p,{children:["Because this call happens during render, it reruns every time ",(0,s.jsx)(t.code,{children:"Ticket"})," re-renders, which is exactly what the ",(0,s.jsx)(t.code,{children:"now"})," state we added to ",(0,s.jsx)(t.code,{children:"TicketControl"})," is for: every minute, ",(0,s.jsx)(t.code,{children:"TicketControl"})," re-renders, ",(0,s.jsx)(t.code,{children:"Ticket"})," re-renders along with it, and ",(0,s.jsx)(t.code,{children:"formatDistanceToNow(timeOpen)"}),' runs again with a fresh idea of "now".']}),"\n",(0,s.jsxs)(t.p,{children:["While we were in here, we also dropped the fragment that used to wrap the ",(0,s.jsx)(t.code,{children:"<div>"}),". A fragment is only needed when a component returns more than one element at the top level, and this component returns a single ",(0,s.jsx)(t.code,{children:"<div>"}),"."]}),"\n",(0,s.jsxs)(t.p,{children:[(0,s.jsx)(t.code,{children:"TicketDetail"})," needs no changes at all. It receives a whole ",(0,s.jsx)(t.code,{children:"ticket: TicketData"})," object rather than individual props, so it automatically has access to ",(0,s.jsx)(t.code,{children:"timeOpen"}),". If we later decide to show the wait time on the detail page too, that's a one-line addition with no prop plumbing."]}),"\n",(0,s.jsxs)(t.h3,{id:"keeping-editticketform-compiling",children:["Keeping ",(0,s.jsx)(t.code,{children:"EditTicketForm"})," Compiling"]}),"\n",(0,s.jsxs)(t.p,{children:["Now that ",(0,s.jsx)(t.code,{children:"TicketData"})," requires a ",(0,s.jsx)(t.code,{children:"timeOpen"})," field, the object ",(0,s.jsx)(t.code,{children:"EditTicketForm"})," builds when a user submits an edit needs to include it too, or this call to ",(0,s.jsx)(t.code,{children:"onEditTicket()"})," won't compile:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/EditTicketForm.tsx"',children:"function handleEditTicketFormSubmission(event: SubmitEvent<HTMLFormElement>) {\n  event.preventDefault();\n  const formData = new FormData(event.currentTarget);\n  onEditTicket({\n    names: formData.get('names') as string,\n    section: formData.get('section') as string,\n    issue: formData.get('issue') as string,\n    id: ticket.id,\n    // highlight-next-line\n    timeOpen: ticket.timeOpen\n  });\n}\n"})}),"\n",(0,s.jsxs)(t.p,{children:["Just like ",(0,s.jsx)(t.code,{children:"id"}),", ",(0,s.jsx)(t.code,{children:"timeOpen"})," isn't something a user edits through the form, so we carry it over from the existing ",(0,s.jsx)(t.code,{children:"ticket"})," prop rather than reading it from ",(0,s.jsx)(t.code,{children:"formData"}),". Editing a ticket's names, section, or issue shouldn't reset when it was originally opened."]}),"\n",(0,s.jsxs)(t.admonition,{title:"This fix is about TypeScript, not a runtime bug",type:"note",children:[(0,s.jsxs)(t.p,{children:["Without this change, the app would actually keep working fine. ",(0,s.jsx)(t.code,{children:"handleEditTicket"})," in ",(0,s.jsx)(t.code,{children:"TicketControl"})," destructures ",(0,s.jsx)(t.code,{children:"id"})," off the object it receives and passes the rest straight to ",(0,s.jsx)(t.code,{children:"updateDoc()"}),", which only touches the fields you give it. So a missing ",(0,s.jsx)(t.code,{children:"timeOpen"})," wouldn't get sent to Firestore at all, and the ticket's existing ",(0,s.jsx)(t.code,{children:"timeOpen"})," would simply be left alone in the database - which is what we want anyway."]}),(0,s.jsxs)(t.p,{children:["The problem is purely that ",(0,s.jsx)(t.code,{children:"onEditTicket"})," is typed to expect a full ",(0,s.jsx)(t.code,{children:"TicketData"}),", so leaving ",(0,s.jsx)(t.code,{children:"timeOpen"})," out won't compile. Vite's dev server doesn't type-check, so ",(0,s.jsx)(t.code,{children:"npm run dev"})," would run without complaint; only ",(0,s.jsx)(t.code,{children:"tsc"})," (via ",(0,s.jsx)(t.code,{children:"npm run build"}),", or your editor) catches it. We're fixing it here because a real project should still compile cleanly, not because skipping it would break anything you'd actually see in the browser."]})]}),"\n",(0,s.jsx)(t.p,{children:"At this point, we've completed our refactor. Serve your Help Queue and test out the changes we made; you should now see a formatted time listed that shows how long a ticket has been open."}),"\n",(0,s.jsx)(t.h2,{id:"ordering-tickets-by-creation-timestamp",children:"Ordering Tickets by Creation Timestamp"}),"\n",(0,s.jsx)(t.hr,{}),"\n",(0,s.jsxs)(t.p,{children:["Now that we have a timestamp associated with each ticket, let's sort our tickets by their creation date. To do this we need to change our ",(0,s.jsx)(t.code,{children:"onSnapshot()"})," Firestore listener in ",(0,s.jsx)(t.code,{children:"TicketControl"})," to use a ",(0,s.jsx)(t.code,{children:"query()"}),"."]}),"\n",(0,s.jsx)(t.p,{children:"Here's the updated code:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",metastring:'title="src/components/TicketControl.tsx"',children:"// ...other imports\n// highlight-next-line\nimport { collection, addDoc, doc, updateDoc, onSnapshot, deleteDoc, query, orderBy } from 'firebase/firestore';\n\nfunction TicketControl() {\n  // ...other state declarations\n\n  useEffect(() => {\n    // highlight-start\n    const queryByTimestamp = query(\n      collection(db, \"tickets\"),\n      orderBy('timeOpen')\n    );\n    // highlight-end\n    const unSubscribe = onSnapshot(\n      // highlight-next-line\n      queryByTimestamp,\n      (collectionSnapshot) => {\n        const tickets: TicketData[] = [];\n        collectionSnapshot.forEach((doc) => {\n          const data = doc.data({ serverTimestamps: 'estimate' });\n          const jsDate = data.timeOpen.toDate();\n          tickets.push({\n            names: data.names,\n            section: data.section,\n            issue: data.issue,\n            timeOpen: jsDate,\n            id: doc.id\n          });\n        });\n        setMainTicketList(tickets);\n      },\n      (firestoreError) => {\n        setError(firestoreError.message);\n      }\n    );\n\n    return () => unSubscribe();\n  }, []);\n\n  // ...rest of TicketControl\n}\n\nexport default TicketControl;\n"})}),"\n",(0,s.jsxs)(t.p,{children:["We start by updating our import statement from ",(0,s.jsx)(t.code,{children:"firebase/firestore"})," to also import ",(0,s.jsx)(t.code,{children:"query"})," and ",(0,s.jsx)(t.code,{children:"orderBy"}),"."]}),"\n",(0,s.jsx)(t.p,{children:"Then, in our effect, we start by constructing a query:"}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"const queryByTimestamp = query(\n  collection(db, \"tickets\"),\n  orderBy('timeOpen')\n);\n"})}),"\n",(0,s.jsxs)(t.p,{children:["The ",(0,s.jsx)(t.code,{children:"queryByTimestamp"})," query gets all documents in the ",(0,s.jsx)(t.code,{children:'"tickets"'})," collection and orders them by the value set in each ticket's ",(0,s.jsx)(t.code,{children:"timeOpen"})," field. The order of the tickets will be ascending: older tickets will be at the top and newer tickets will be at the bottom."]}),"\n",(0,s.jsxs)(t.p,{children:["If we wanted descending order instead, we could specify that by adding a second argument to the ",(0,s.jsx)(t.code,{children:"orderBy()"})," function:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"const queryByTimestamp = query(\n  collection(db, \"tickets\"),\n  orderBy('timeOpen', 'desc')\n);\n"})}),"\n",(0,s.jsxs)(t.p,{children:["The only other change we make is updating the first argument in the ",(0,s.jsx)(t.code,{children:"onSnapshot()"})," function call to use the ",(0,s.jsx)(t.code,{children:"queryByTimestamp"})," variable, which represents our Firestore query:"]}),"\n",(0,s.jsx)(t.pre,{children:(0,s.jsx)(t.code,{className:"language-tsx",children:"useEffect(() => {\n  const queryByTimestamp = query(\n    collection(db, \"tickets\"),\n    orderBy('timeOpen')\n  );\n  const unSubscribe = onSnapshot(\n    // highlight-next-line\n    queryByTimestamp,\n    // ...same callbacks as before\n  );\n\n  return () => unSubscribe();\n}, []);\n"})}),"\n",(0,s.jsx)(t.p,{children:"Nothing inside the success callback needed to change. Firebase types a query and a collection reference so that both produce the same kind of snapshot, which means our existing callback keeps working unmodified."}),"\n",(0,s.jsx)(t.p,{children:"And that's it! Now our tickets are organized by their creation date."}),"\n",(0,s.jsx)(t.p,{children:"The best thing about this refactor is that we didn't have to write our own function to sort the Firestore data. That's a big advantage of using Firestore: it's flexible like all NoSQL databases are, but it contains enough structure (in the form of collections and documents) and built-in helper functions to make filtering and sorting data straightforward."}),"\n",(0,s.jsx)(t.p,{children:"Next up, let's learn how to host our Help Queue web app with Firebase."})]})}function h(e={}){const{wrapper:t}={...(0,r.R)(),...e.components};return t?(0,s.jsx)(t,{...e,children:(0,s.jsx)(l,{...e})}):l(e)}},28453(e,t,n){n.d(t,{R:()=>o,x:()=>a});var i=n(96540);const s={},r=i.createContext(s);function o(e){const t=i.useContext(r);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:o(e.components),i.createElement(r.Provider,{value:t},e.children)}}}]);